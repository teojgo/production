diff -Nwaru cosmo_pompa.orig/cosmo/Options cosmo_pompa/cosmo/Options
--- cosmo_pompa.orig/cosmo/Options	2020-09-07 11:38:33.000000000 +0200
+++ cosmo_pompa/cosmo/Options	2022-02-08 17:11:13.641947000 +0100
@@ -1,17 +1,15 @@
 # compiler
-F90      = ftn -D__CRAY_FORTRAN__
+F90 = ftn -D__CRAY_FORTRAN__ -homp -h system_alloc -add-rpath -L$(CUDATOOLKIT_HOME)/lib64 -lcudart -lcublas
 COMPILER = cray
 
 # linker
 LD       = $(F90)
 
--include $(ROOT)/Options.$(COMPILER).cpu
+-include $(ROOT)/Options.$(COMPILER).gpu
 
 #PFLAGS  += -DSINGLEPRECISION # run model in single precision
 
--include $(ROOT)/Options.lib.cpu
+-include $(ROOT)/Options.lib.gpu
 
-# bit-repro math
-CMATH_DIR=/users/charpill/prg/mch/cmath
-FMATH_DIR=/users/charpill/prg/mch/fmath
+INC += -I$(MPICH_DIR)/include
 CC = CC
\ No newline at end of file
diff -Nwaru cosmo_pompa.orig/cosmo/Options.cray.gpu cosmo_pompa/cosmo/Options.cray.gpu
--- cosmo_pompa.orig/cosmo/Options.cray.gpu	2020-09-07 11:38:33.000000000 +0200
+++ cosmo_pompa/cosmo/Options.cray.gpu	2022-02-08 17:06:44.104373000 +0100
@@ -14,9 +14,6 @@
 FFLAGS  += -hadd_paren # automatically adds parenthesis to select associative operations
 FFLAGS  += -hacc # compile for GPU
 FFLAGS  += -ra # generates listing
-#MED 26/11/2019>>
-FFLAGS  += -h system_alloc -add-rpath
-#MED<<
 
 # verbose compilation flags
 VFLAGS   = -v # show compilation information
diff -Nwaru cosmo_pompa.orig/cosmo/Options.lib.gpu cosmo_pompa/cosmo/Options.lib.gpu
--- cosmo_pompa.orig/cosmo/Options.lib.gpu	2020-09-07 11:38:33.000000000 +0200
+++ cosmo_pompa/cosmo/Options.lib.gpu	2022-02-08 17:04:00.142713000 +0100
@@ -2,9 +2,6 @@
 
 # global libraries and includes
 LIB      = $(LDFLAGS)
-#MED 26/11/2019>>
-LIB     += -L/opt/nvidia/cudatoolkit10/10.0.130_3.22-7.0.1.0_5.2__gdfb4ce5/lib64 -lcudart -lcublas
-#MED<<
 INC      = -I.
 INC     += -I$(ROOT)/$(SRCDIR)
 
@@ -21,35 +18,34 @@
 MPII     = -D__MPICH2
 
 # Grib1 library
-GRIBDWDL = -L$(INSTALL_DIR)/libgrib1 -lgrib1_$(COMPILER)
+GRIBDWDL = -L${EBROOTLIBGRIB1_CRCLIM} -lgrib1_cray
 GRIBDWDI =
 
 # Grib-API library
-GRIBAPI_VERSION = v1.20.0.2
-GRIBAPIL = -L$(INSTALL_DIR)/libgrib_api/$(GRIBAPI_VERSION)/$(COMPILER)/lib -lgrib_api_f90 -lgrib_api -L$(INSTALL_DIR)/libjasper/lib -ljasper
-GRIBAPII = -I$(INSTALL_DIR)/libgrib_api/$(GRIBAPI_VERSION)/$(COMPILER)/include
+GRIBAPI_VERSION = v1.13.1
+GRIBAPIL = -L${EBROOTGRIB_API}/lib -lgrib_api_f90 -lgrib_api -L${EBROOTJASPER}/lib -ljasper
+GRIBAPII = -I${EBROOTGRIB_API}/include
 
 # NetCDF library
-NETCDFL  = 
-NETCDFI  = 
+NETCDFL  = -L${NETCDF_DIR}/lib
+NETCDFI  = -I${NETCDF_DIR}/include
 
 # Synsat library
-RTTOV7L  = -L$(INSTALL_DIR)/librttov7 -lrttov7_$(COMPILER)
+# RTTOV7L  = -L$(INSTALL_DIR)/librttov7 -lrttov7_$(COMPILER)
 RTTOV7I  =
 RTTOV10L =
 RTTOV10I =
 
 # Serialization library
-SERIALBOX  =
-SERIALBOXL =
-SERIALBOXI =
+SERIALBOX  = ${EBROOTSERIALBOX}
+SERIALBOXL = -L${EBROOTSERIALBOX}/lib
 
 # STELLA library
-STELLA   = $(STELLA_DIR)
-STELLAL  = -L$(STELLA)/lib -lCommunicationFrameworkCUDA -ljson -lStellaCUDA -lgcl -lStellaUtils -lSharedInfrastructureCUDA -lstdc++
-STELLAI  =
+STELLA   = ${EBROOTSTELLA}
+STELLAL  = -L${EBROOTSTELLA}/lib -lCommunicationFrameworkCUDA -ljson -lStellaCUDA -lgcl -lStellaUtils -lSharedInfrastructureCUDA -lstdc++
+STELLAI  = -I${EBROOTSTELLA}/include/STELLA
 
 # Dycore library
-DYCORE   = $(DYCORE_DIR)
-DYCOREL  = -L$(DYCORE)/lib -lDycoreWrapperCUDA -lDycoreCUDA
+DYCORE   = ${EBROOTDYCORE}
+DYCOREL  = -L${EBROOTDYCORE}/lib -lDycoreWrapperCUDA -lDycoreCUDA
 DYCOREI  =
diff -Nwaru cosmo_pompa.orig/cosmo/src/src_block_fields.f90 cosmo_pompa/cosmo/src/src_block_fields.f90
--- cosmo_pompa.orig/cosmo/src/src_block_fields.f90	2020-09-07 11:38:33.000000000 +0200
+++ cosmo_pompa/cosmo/src/src_block_fields.f90	2022-02-08 16:38:36.136556362 +0100
@@ -1398,6 +1398,8 @@
 ! ---------------- 
   INTEGER :: ip, i, j, k
 
+!DIR$ INLINENEVER copyToBlock3d
+
 !------------------------------------------------------------------------------
 ! Begin Subroutine  copyToBlock3d
 !------------------------------------------------------------------------------
@@ -1433,6 +1435,8 @@
 ! ---------------- 
   INTEGER :: ip, i, j
 
+!DIR$ INLINENEVER copyToBlock2d
+
 !------------------------------------------------------------------------------
 ! Begin Subroutine  copyToBlock2d
 !------------------------------------------------------------------------------
@@ -1601,6 +1605,8 @@
 ! ---------------- 
   INTEGER :: ip, i, j, k
 
+!DIR$ INLINENEVER copyFromBlock3d
+
 !------------------------------------------------------------------------------
 ! Begin Subroutine  copyFromBlock3d
 !------------------------------------------------------------------------------
@@ -1641,6 +1647,8 @@
 ! ---------------- 
   INTEGER :: ip, i, j
 
+!DIR$ INLINENEVER copyFromBlock2d
+
 !------------------------------------------------------------------------------
 ! Begin Subroutine  copyFromBlock2d
 !------------------------------------------------------------------------------
diff -Nwaru cosmo_pompa.orig/cosmo/src/src_output.f90 cosmo_pompa/cosmo/src/src_output.f90
--- cosmo_pompa.orig/cosmo/src/src_output.f90	2020-09-07 11:38:33.000000000 +0200
+++ cosmo_pompa/cosmo/src/src_output.f90	2022-02-08 16:40:24.715266432 +0100
@@ -5577,9 +5577,9 @@
 !RUS_CHANGE/REPLACE<
 !#  IF ( MINVAL(outblock%plev(1:outblock%kepin)) <= MINVAL(pexp(1:ie,1:je,1)) ) THEN
 !RUS_CHANGE/REPLACE-
-    !$acc kernels
+    !$acc parallel copy(pexp_min, pexp) num_gangs(1) vector_length(1)
     pexp_min = pexp(istartpar,jstartpar,1)
-    !$acc end kernels
+    !$acc end parallel
 
     !$acc parallel
     !$acc loop gang reduction (min:pexp_min)
